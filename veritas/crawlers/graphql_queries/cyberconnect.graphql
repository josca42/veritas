query ($address: String!) {
  identity(address: $address) {
    address
    followingCount
    followerCount
    followers {
      list {
        address
      }
    }
    followings {
      list {
        address
      }
    }
  }
}
